import { SageDirective } from './sage.directive';

describe('SageDirective', () => {
  it('should create an instance', () => {
    const directive = new SageDirective();
    expect(directive).toBeTruthy();
  });
});
